const fs = require('fs');
const input = fs.readFileSync(0,'utf-8').toString().trim();

/* 
MenOfPassion(A[], n) {
    sum <- 0;
    for i <- 1 to n
        sum <- sum + A[i]; # 코드1
    return sum;
}
} */

const n = input;
console.log(n);
console.log(1);